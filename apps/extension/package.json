{
  "name": "@nfe-agendamento/extension",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "npm run typecheck && node scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "vitest": "5.0.0",
    "typescript": "7.0.2"
  }
}
